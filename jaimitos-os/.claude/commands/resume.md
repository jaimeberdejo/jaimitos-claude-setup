Read docs/SPEC.md, docs/ROADMAP.md and docs/STATE.md.
Summarize in two lines where we are. State the single next action — and whether the next open
phase is loopable or supervised (check its `Mode:` line), so we pick `/autopilot` vs a supervised
`/phase` correctly.
Then wait for my go-ahead — do not start coding.
